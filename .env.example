# ══════════════════════════════════════════════════════════════════════════════
#  Environment Variables (used by docker-compose.local.yml)
# ══════════════════════════════════════════════════════════════════════════════
#
#  Copy this file to .env and fill in the values:
#    cp .env.example .env
#
#  The init script (init.sh / init.ps1) does this automatically and generates
#  secure defaults for you.
#
#  IMPORTANT: Never commit .env to source control — it is git-ignored.
# ══════════════════════════════════════════════════════════════════════════════

# ── Authentication ───────────────────────────────────────────────────────────
# JWT signing key for authentication tokens.
# Must be a long, random string (minimum 32 characters).
# In production, set this via the environment variable: Authentication__Jwt__Key
JWT_SECRET_KEY=<generate-a-random-secret-here>

# ── PostgreSQL ───────────────────────────────────────────────────────────────
POSTGRES_USER=admin
POSTGRES_PASSWORD=pwd

# ── Redis ────────────────────────────────────────────────────────────────────
REDIS_PASSWORD=redis-dev-password
